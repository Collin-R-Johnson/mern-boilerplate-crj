version: '3'
services:

  server:
    container_name: express-server
    build: ./server/
    ports:
      - "80:3000"
    volumes:
      - ./server:/usr/src/app
    links:
      - mongo
  frontend:
    container_name: react-frontend
    build: ./client/
    ports:
      - "3001:3001"
    volumes:
      - ./client:/usr/src/app
    depends_on:
      - server
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - "27017:27017"